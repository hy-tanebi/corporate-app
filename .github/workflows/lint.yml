name: Lint with Biome
on:
  pull_request:
    branches: [dev]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Always succeed
        run: echo "Final attempt to register the check."
